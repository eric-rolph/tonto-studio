import {freshPatch,validatePatch,controls} from './model.js';
export class Engine extends EventTarget{
 constructor(){super();this.state=freshPatch();this.notes=new Map();this.released=new Set();this.sustain=false;this.micStream=null;}
 get params(){return this.state.params;}get routes(){return this.state.routes;}
 async start(){if(this.starting)return this.starting;this.starting=this.initialize();try{await this.starting;}finally{this.starting=null;}}
 async initialize(){await this.recoveryReady;
  if(this.ctx){await this.ctx.resume();return;}if(!window.AudioContext)throw new Error('Use a browser with Web Audio, such as Chrome or Edge.');
  const ctx=this.ctx=new AudioContext({latencyHint:'interactive',sampleRate:48000});
  try{await ctx.audioWorklet.addModule('/dsp.js');}catch(e){await ctx.close();this.ctx=null;throw e;}
  this.synth=new AudioWorkletNode(ctx,'tonto-console',{numberOfInputs:1,numberOfOutputs:4,outputChannelCount:[2,1,1,4]});
  this.bus=ctx.createGain();this.master=ctx.createGain();this.master.gain.value=this.params.master;
  this.limiter=ctx.createDynamicsCompressor();this.limiter.threshold.value=-3;this.limiter.knee.value=0;this.limiter.ratio.value=20;this.limiter.attack.value=.002;this.limiter.release.value=.12;
  this.analyser=ctx.createAnalyser();this.analyser.fftSize=2048;this.micAnalyser=ctx.createAnalyser();this.micAnalyser.fftSize=2048;
  this.synthMonitor=ctx.createGain();this.synth.connect(this.synthMonitor,0);this.synthMonitor.connect(this.bus);this.quadScope=ctx.createAnalyser();this.quadScope.fftSize=2048;this.scopeMute=ctx.createGain();this.scopeMute.gain.value=0;this.synth.connect(this.quadScope,0);this.quadScope.connect(this.scopeMute).connect(ctx.destination);this.synth.connect(this.micAnalyser,1);this.bus.connect(this.master).connect(this.limiter).connect(this.analyser).connect(ctx.destination);
  this.clickMonitor=ctx.createGain();this.clickMonitor.gain.value=.65;this.synth.connect(this.clickMonitor,2);this.clickMonitor.connect(this.master);
  this.synth.port.onmessage=({data})=>this.dispatchEvent(new CustomEvent('meter',{detail:data}));
  this.synth.onprocessorerror=()=>this.dispatchEvent(new CustomEvent('error',{detail:'The audio processor stopped. Export your recordings, then reload to restart it.'}));
  this.configure();await ctx.resume();this.dispatchEvent(new Event('ready'));
 }
 setQuad(enabled){const ctx=this.ctx;if(enabled&&ctx.destination.maxChannelCount<4)throw new Error('Four output channels are unavailable.');this.quadEnabled=enabled;this.synthMonitor.gain.setValueAtTime(enabled?0:1,ctx.currentTime);if(!this.quadMonitor){this.quadMonitor=ctx.createGain();this.quadMonitor.channelCount=4;this.quadMonitor.channelCountMode='explicit';this.quadMonitor.channelInterpretation='discrete';this.synth.connect(this.quadMonitor,3);this.quadMonitor.connect(ctx.destination);}ctx.destination.channelCount=enabled?4:2;ctx.destination.channelInterpretation='discrete';this.quadMonitor.gain.setValueAtTime(enabled?this.params.master:0,ctx.currentTime);}
 send(type,data={}){this.synth?.port.postMessage({type,...data});}
 configure(){this.send('configure',{state:this.state});this.dispatchEvent(new Event('patchchange'));}
 load(patch){this.panic();this.state=validatePatch(patch);this.configure();this.master?.gain.setTargetAtTime(this.params.master,this.ctx.currentTime,.02);this.dispatchEvent(new Event('patchload'));}
 set(key,value){if(key==='tempo'&&this.performanceTempo!=null)return;const c=controls[key];if(!c||!Number.isFinite(value))return;value=Math.max(c.min,Math.min(c.max,value));const previous=this.params[key];if(value===previous)return;this.params[key]=value;if(key==='master'&&this.quadMonitor)this.quadMonitor.gain.setTargetAtTime(this.quadEnabled?value:0,this.ctx.currentTime,.015);if(key==='master')this.master?.gain.setTargetAtTime(value,this.ctx.currentTime,.015);this.send('params',{values:{[key]:value}});this.dispatchEvent(new CustomEvent('parameter',{detail:{key,value,previous}}));this.dispatchEvent(new Event('patchchange'));}
 patch(dest,source){if(source){this.routes[dest]=source;this.state.cables[dest]={adapter:false,scale:false,trigger:false,gain:1};}else {delete this.routes[dest];delete this.state.cables[dest];}this.configure();}
 trigger(retrigger=true,changedPart){const groups=[null,...Object.keys(this.state.parts||{}).filter(id=>this.state.parts[id].enabled)];for(const part of groups){if(changedPart!==undefined&&part!==changedPart)continue;const notes=[...this.notes.values()].filter(n=>(n.part||null)===part),last=notes.at(-1),data=last?{...last,retrigger,lower:Math.min(...notes.map(n=>n.note)),upper:Math.max(...notes.map(n=>n.note))}:null;if(part)this.send('part-live',{part,note:data});else if(data)this.send('on',data);else this.send('off');}}
 on(note,velocity=1,id=note,target){const part=target===false?null:target??(this.state.parts?.[this.activePart]?.enabled?this.activePart:null);this.notes.delete(id);this.notes.set(id,{note,velocity,part});this.released.delete(id);this.trigger(true,part);this.dispatchEvent(new CustomEvent('noteaction',{detail:{on:true,note,velocity,id,part}}));this.dispatchEvent(new Event('notes'));}
 off(id){if(!this.notes.has(id))return;const part=this.notes.get(id).part||null;if(this.isSustained(id)){this.released.add(id);return;}this.notes.delete(id);this.released.delete(id);this.trigger(false,part);this.dispatchEvent(new CustomEvent('noteaction',{detail:{on:false,id}}));this.dispatchEvent(new Event('notes'));}
 isSustained(id){return [...(this.pedals||[])].some(owner=>!owner.startsWith('midi-')||String(id).startsWith(owner+'-'));}
 sustainPedal(on,owner='manual'){this.pedals??=new Set();if(on)this.pedals.add(owner);else this.pedals.delete(owner);this.sustain=this.pedals.size>0;for(const id of this.released)if(!this.isSustained(id))this.off(id);}
 panic(){this.notes.clear();this.released.clear();this.sustain=false;this.pedals?.clear();this.state.sequencer=false;this.send('panic');this.dispatchEvent(new Event('panic'));this.dispatchEvent(new Event('notes'));}
 async microphone(deviceId){await this.start();this.stopMicrophone();if(!navigator.mediaDevices?.getUserMedia)throw new Error('Microphone access needs HTTPS and browser permission.');this.micStream=await navigator.mediaDevices.getUserMedia({audio:{deviceId:deviceId?{exact:deviceId}:undefined,echoCancellation:false,noiseSuppression:false,autoGainControl:false,channelCount:1},video:false});this.micSource=this.ctx.createMediaStreamSource(this.micStream);this.micSource.connect(this.synth);this.micStream.getAudioTracks()[0].onended=()=>{this.stopMicrophone();this.dispatchEvent(new Event('micended'));};}
 stopMicrophone(){this.micSource?.disconnect();this.micStream?.getTracks().forEach(t=>t.stop());this.micStream=null;this.micSource=null;}
 async midi(){await this.start();if(!navigator.requestMIDIAccess)throw new Error('Web MIDI is unavailable. Use Chrome or Edge, or the on-screen keyboard.');this.midiAccess=await navigator.requestMIDIAccess({sysex:false});this.bindMidi();this.midiAccess.onstatechange=()=>this.bindMidi();return this.midiAccess.inputs.size;}
 dropNotes(predicate){let changed=false;for(const id of this.notes.keys())if(predicate(id)){this.notes.delete(id);this.released.delete(id);this.dispatchEvent(new CustomEvent('noteaction',{detail:{on:false,id}}));changed=true;}if(changed){this.trigger(false);this.dispatchEvent(new Event('notes'));}}
 bindMidi(){
    const inputs=[...this.midiAccess.inputs.values()].filter(input=>input.state!=='disconnected');
    for(const input of this.boundMidi||[])if(!inputs.some(next=>next.id===input.id)){input.onmidimessage=null;this.dropNotes(id=>String(id).startsWith('midi-'+input.id+'-'));for(const owner of this.pedals||[])if(owner.startsWith('midi-'+input.id+'-'))this.sustainPedal(false,owner);}
    this.boundMidi=inputs;for(const input of inputs)input.onmidimessage=({data,timeStamp})=>{if(data[0]>=240){this.dispatchEvent(new CustomEvent('midirealtime',{detail:{device:input.id,data:Array.from(data),time:timeStamp??performance.now()}}));return;}const [status,n,v]=data,type=status&240,id=`midi-${input.id}-${status&15}-${n}`;if(type===144&&v>0)this.routeMidiOn?this.routeMidiOn(n,v/127,id,status&15):this.on(n,v/127,id,false);if(type===128||(type===144&&!v))(this.routeMidiOff?this.routeMidiOff(id):this.off(id));if(type===224)this.set('bend',(((v<<7)|n)-8192)/8192*2);if(type===176){if(n===64)this.sustainPedal(v>=64,`midi-${input.id}-${status&15}`);if(n===120||n===123)this.panic();const useDefault=this.dispatchEvent(new CustomEvent('midicc',{cancelable:true,detail:{device:input.id,name:input.name,channel:status&15,cc:n,value:v}}));if(useDefault){if(n===1||n===11)this.set('expression',v/127);if(n===7)this.set('master',v/127);}this.dispatchEvent(new Event('control'));}};this.dispatchEvent(new CustomEvent('midistate',{detail:inputs.map(i=>i.name)}));}
}
